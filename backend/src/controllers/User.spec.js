const expect = require('chai').expect;
const sinon = require('sinon');
const proxyquire = require('proxyquire');

describe('CONTROLLER', () => {
  describe('User', () => {
    it('', () => {
      
    });
  });
});